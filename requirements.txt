# NeuroForge 3D - SPRINT 1 Requirements
# Based on Microsoft TRELLIS dependencies
# Compatible with Python 3.10+ and CUDA 12.1

# Core PyTorch (will be installed separately in Dockerfile for CUDA compatibility)
# torch==2.4.0
# torchvision==0.19.0

# Image Processing
pillow>=10.3.0
imageio>=2.31.0
imageio-ffmpeg>=0.4.9
opencv-python-headless>=4.8.1.78

# Scientific Computing
numpy>=1.24.0
scipy>=1.11.0
tqdm>=4.66.0

# 3D Processing - Core focus for watertight STL validation
trimesh>=4.0.0
open3d>=0.18.0
xatlas>=0.0.9
pyvista>=0.43.0
pymeshfix>=0.16.0
igraph>=0.11.0

# AI/ML Libraries
transformers>=4.48.0
diffusers>=0.24.0
safetensors>=0.4.0

# Utilities
easydict>=1.11
ninja>=1.11.0
rembg>=2.0.0
onnxruntime>=1.16.0

# Web Interface (Gradio for SPRINT 4, but including early for testing)
# NOTE: Using version 5.11.0+ for most security fixes
# WARNING: Known DoS vulnerability in multipart boundary upload (affects <= 5.22.0, no patch available yet)
# This is acceptable for SPRINT 1 as Gradio is not yet deployed in production
gradio>=5.11.0

# Development Tools
tensorboard>=2.15.0

# Additional utilities from TRELLIS
einops>=0.7.0
huggingface-hub>=0.19.0
